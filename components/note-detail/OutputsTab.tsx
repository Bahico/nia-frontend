import { StyleSheet, View } from 'react-native';

export function OutputsTab() {
  return <View style={styles.outputsEmpty} />;
}

const styles = StyleSheet.create({
  outputsEmpty: {
    height: 1,
  },
});

