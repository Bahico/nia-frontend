/**
 * Change user password.
 * Endpoint not implemented yet – placeholder that performs no request.
 */
export async function changePassword(_currentPassword: string, _newPassword: string): Promise<void> {
  // TODO: call API when change-password endpoint is available
  return Promise.resolve();
}
